//ISSO eh diferente de isso
// aqui e pra calcular a area do retangulo
/*
a = b * h

area_retangulo = base * altura

main () {
  linha_de_codigo;
}
*/

#include <stdio.h> //inclusao de biblioteca

int main() { //função principal
  // seu codigo aqui

  // 1.Entrada
  // levantar meus ingredientes
  // char, int(intero), float (flutuante, decimal)
  // variaveis
  // leitura
  int primeiro_numero = 25;
  int segundo_numero = 64;
  // primeiro_numero = 5;
  // segundo_numero = 6;
  
  // 2.Processamento
  // calculo: soma, subtraçao, multiplicacao, divisao
  int soma = primeiro_numero + segundo_numero;
  
  // 3.Saida
  // escrita (mostrar resultado)
  printf("A soma de %i com %i eh %i\n", primeiro_numero, segundo_numero, soma);    
  
  return 0; // return fim do programa
  }